# Email Payload Validation Challenge

Implement robust validation for sender/receiver **names**, **email addresses**, and an **HTML email payload** with placeholders.

## Install & run

```bash
python -m venv .venv
. .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
pytest -q
```
